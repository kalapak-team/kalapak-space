# Production — www.kalapak-team.space (frontend) + www.api.kalapak-team.space (API)
# Relative /api works when nginx proxies to BACKEND_URL; use absolute URL if API is called cross-origin only.
VITE_API_URL=/api
NUXT_PUBLIC_API_URL=/api
NUXT_PUBLIC_SITE_URL=https://www.kalapak-team.space
VITE_TURNSTILE_SITE_KEY=0x4AAAAAAAC4BOzhJInXTTfSR
